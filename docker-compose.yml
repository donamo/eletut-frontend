services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://eletut-api.donamo.science}
    image: eletut-frontend:latest
    container_name: eletut_frontend_nginx
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- --timeout=2 http://localhost:80/login.html | grep -q '<html' || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
    environment:
      - VIRTUAL_HOST=eletut.donamo.science
      - LETSENCRYPT_HOST=eletut.donamo.science
      - VIRTUAL_PORT=80

networks:
  default:
    name: nginx-proxy
    external: true
